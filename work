import json
import time
amster={}
temp={}
x=input("do want to enter something into database and perform crude operations on it yes/no:")
if(x=="yes"):
    print("database is")
    with open('master json','r') as masteropen:
        data_load=json.load(masteropen)
    temp+=data_load
    print("loaded database is ")
    print(temp)
d=temp
def read(k):
    if k not in d:
        print("error")
    else:
        b=d[k]
        if b[1]!=0:
            if time.time().b[1]:
                str1=str[k]+"1"+str(b[0])
                return str1
        else:
            print("error")
x=input("do you want to enter someting into database and perform crude operations on it yes/no:")
            if(x=="yes"):
                for i in range[0,5]:
                    print("choose from 1.create 2.read 3.delete 4.pass 5.print")
                    if(x==4):
                        break
                    if(x==1):
                        key=input("enter data")
                        value=int(input("enter value"))
                        create(key,value)
                    if(x==2):
                        key=input("enter key")
                        print(read(str(key)))
                    if(x==3):
                        key=input("enter key")
                        delete(key)
                        print("deleted")
                    if(x==5):
                        print(d)
impot json
temp+=d
with open('temp,json','w') as fp:
    json.dump(temp,fp)
print("database operations are")
print(temp)
x=input("is it your first operation yes/no:")
if(x=="yes"):
    with open('master.json','w') as fp:
        json.dump(temp,fp)
    print("thanks bro")
    exit()
x=input("do you want to save yes/no:")
if(x=="yes"):
    data={}
    import json
    with open('master,json','r') as fp:
        data=json.load(fp)
    master=dist(data)
    master.update(temp)
    with open('master,json','w') as fp:
        json.dump(master,fp)
        
print("everything is done,thanks")
